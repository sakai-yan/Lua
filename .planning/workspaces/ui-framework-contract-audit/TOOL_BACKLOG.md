# Tool Backlog

## Candidate Tools
- `ui-api-contract-lint`: scan `Code/Core/UI` for property-name drift between component classes, `Style`, `Builder`, and docs.
- `ui-bootstrap-surface-check`: compare `Main.lua`, `Init.j`, and documented module surfaces for drift.
- `ui-audit-smoke-fixture`: minimal scripted UI fixture for future V1/V1.5 verification rounds.

## Approved Builds
- None yet. These tools should be reconsidered once V1 implementation starts.

## Rejected Ideas
- Heavy runtime benchmark tooling was rejected for this round because the user asked for a static-first, low-cost audit.
