# Tasks

## Active
- Work currently being executed
- Keep the default continuation target as the first actionable item here.
- Do not leave clearly in-scope, currently feasible implementation work here as outline-only progress.
- When debug-loop work is active, keep the current debug continuation target as the first actionable item here.

## Ready
- If any archived thread resumes, create a dedicated sibling workspace for it before adding new planning state.
- If any archived report is still operationally important, refresh it into a dedicated task workspace instead of extending this archive.

## Blocked
- Original per-task boundaries cannot be reconstructed with full confidence from the legacy root workspace alone.

## Done
- Archived the remaining point, packer, benchmark, and xlik-related artifacts into this workspace during the `.planning` migration.
- Cleaned the root `.planning` container so active work now lives only in dedicated task workspaces.
