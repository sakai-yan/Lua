# Principles

## Active Task Principles
- Prefer the smallest state model that fully owns the invariants.
- A fixed-slot container should expose fixed-slot semantics directly instead of hiding them behind generic container abstractions.
- Do not import shape or behavior from a deprecated sibling implementation just because it exists nearby.

## Candidate Promotions
- None yet

