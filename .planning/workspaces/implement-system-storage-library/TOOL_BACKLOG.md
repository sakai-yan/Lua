# Tool Backlog

## Candidate Tools
- A reusable Lua smoke-test harness for isolated `Logic.System` libraries could reduce one-off inline scripts if similar tasks appear again.

## Deferred on Purpose
- No task-local helper script was created because a single inline smoke run was sufficient for this scope.

