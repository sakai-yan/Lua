# Principles

## Active Principles
- Prefer validator-backed compatibility gates before trusting reconstructed `war3map.wtg/wct` output. Check with the internal validator plus the original YDWE checker when a slice changes trigger reconstruction behavior.
- When structured GUI recovery stops looking editor-readable, prefer bounded `CustomScriptCode` or whole-trigger custom-text over speculative pseudo-GUI expansion.
- Treat `.canon/YDWE` as the authoritative YDWE runtime and source baseline for this task; archived `.tools/YDWE` references are historical evidence, not current truth.

## Deprecated Principles
- None yet.
